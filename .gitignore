# Local-only project config (not tracked)
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Local credentials / secrets — never commit
*.env
.env
.env.*
*-credentials.json
service-account*.json

# Demo sink output
/out/

# Real tenant registry (config/tenants.example.yaml is the tracked demo/test
# fixture; a real deployment's own registry is not committed)
/config/tenants.yaml

# Editor / OS
.vscode/
.idea/
.DS_Store
